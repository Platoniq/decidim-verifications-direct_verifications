en:
  decidim:
    verifications:
      batch_users:
        admin:
          index:
            title: title
          batch_verifications:
            create:
              success: Successfully imported %{count} items (%{errors} errors)
          new:
            info: emails
            textarea: textarea
            register: register
            authorize: authorize
            submit: submit