es:
  decidim:
    verifications:
      batch_users:
        admin:
          index:
            title: title
          batch_verifications:
            create:
              success: Se importaron correctamente %{count} elementos (%{errors} errores)
          new:
            info: emails
            textarea: textarea
            register: register
            authorize: authorize
            submit: submit
